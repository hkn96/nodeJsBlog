<!DOCTYPE html>
<html lang="en">
  <head>
    <%-include('./inc/header')%>
  </head>
  <body>
    <%-include('./inc/nav')%>

    <section>
      <h2>Content</h2>
      <ul>
        <% blogs.forEach(blog=>{ %>

        <li>
          <a href="#"><%=blog.title%></a>
          <p><%=blog.short%></p>
          <p><%=blog.long%></p>
        </li>
        <%})%>
      </ul>
    </section>
    <%-include('./inc/footer')%>
  </body>
</html>
